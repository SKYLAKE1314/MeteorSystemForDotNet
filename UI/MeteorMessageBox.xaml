<Window x:Class="MeteorMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        SizeToContent="Height"
        Width="460"
        MinHeight="180">
    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="MaterialButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8AB4F8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#15FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Primary Button Style -->
        <Style x:Key="MaterialPrimaryButton" TargetType="Button" BasedOn="{StaticResource MaterialButton}">
            <Setter Property="Background" Value="#8AB4F8"/>
            <Setter Property="Foreground" Value="#202124"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A8C7FA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#669DF6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 增加 Margin 防止抗鋸齒邊緣被裁剪導致鋸齒，並加上輕微陰影提升質感 -->
    <Border Background="#202124" BorderBrush="#3C4043" BorderThickness="1" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.5" ShadowDepth="2"/>
        </Border.Effect>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" MouseLeftButtonDown="Header_MouseLeftButtonDown" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="IconText" Grid.Column="0" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="#8AB4F8" VerticalAlignment="Center" Margin="0,0,12,0" Visibility="Collapsed"/>
                <TextBlock x:Name="TitleBlock" Grid.Column="1" FontSize="18" FontWeight="SemiBold" Foreground="#E8EAED" VerticalAlignment="Center"/>
            </Grid>

            <!-- Body -->
            <ScrollViewer Grid.Row="1" Margin="0,16,0,24" VerticalScrollBarVisibility="Auto" MaxHeight="400">
                <TextBox x:Name="MessageBlock" Background="Transparent" BorderThickness="0" Foreground="#9AA0A6" FontSize="14" TextWrapping="Wrap" IsReadOnly="True" AcceptsReturn="True"/>
            </ScrollViewer>

            <!-- Footer (Buttons) -->
            <StackPanel x:Name="ButtonPanel" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnYes" Content="是" Style="{StaticResource MaterialPrimaryButton}" Click="BtnYes_Click" Visibility="Collapsed"/>
                <Button x:Name="BtnNo" Content="否" Style="{StaticResource MaterialButton}" Click="BtnNo_Click" Visibility="Collapsed"/>
                <Button x:Name="BtnOk" Content="確定" Style="{StaticResource MaterialPrimaryButton}" Click="BtnOk_Click" Visibility="Collapsed"/>
                <Button x:Name="BtnCancel" Content="取消" Style="{StaticResource MaterialButton}" Click="BtnCancel_Click" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
